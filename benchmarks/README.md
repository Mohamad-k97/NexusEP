# Benchmarks

`object/` contains legacy `AbbeySimulation` runner profiles. `arrays/`
contains array-core workloads and profiling helpers. Benchmark modules are not
pytest tests and write generated output only below `artifacts/` or an explicit
caller-provided directory.
