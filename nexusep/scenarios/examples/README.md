# Canonical scenario examples

The authoritative version 1 example is maintained at
`contracts/examples/multizone_dwelling_v1_minimal.json` so the JSON contract,
Python loader, and graph compiler test the same file rather than drifting
copies. Loader tests also create isolated JSONC, external-weather, and synthetic
smoke variants from that fixture.

