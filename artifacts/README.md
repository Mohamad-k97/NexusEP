# Generated artifacts

`baseline/` contains small approved manifests. Generated profiles and
benchmark results belong under `profiles/` and `benchmarks/`; those contents
are ignored by Git. Production source and test modules must not write generated
files under `nexusep/` or `tests/`.
