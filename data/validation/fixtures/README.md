# Derived validation fixtures

Validation category: **verification** (fixture provenance)

Only small derived fixtures whose license permits redistribution belong here.
Every fixture must be produced by a registered preprocessing step and recorded
with SHA-256 and byte size in the corresponding source manifest. A fixture is
never a substitute for preserving the raw-file checksum.
