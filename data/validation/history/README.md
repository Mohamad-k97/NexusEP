# Validation artifact history

This directory preserves compact superseded diagnostics for audit comparison.
Only files under `data/validation/fixtures/` and indexed by the scientific
registry are current result artifacts.
